# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # required for file uploads in FastAPI

# ── PDF Processing ────────────────────────────────────────────────────────────
pypdf==4.2.0                   # extract text from PDF pages

# ── AI / Embeddings / LLM ─────────────────────────────────────────────────────
openai==2.43.0                 # OpenAI embeddings + GPT chat completions

# ── Vector Database ───────────────────────────────────────────────────────────
chromadb==1.5.9                # local vector DB — stores embeddings on disk

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.1       # reads .env into Settings class

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
tiktoken==0.7.0                # count tokens (same tokenizer OpenAI uses)

# ── Optional: Ollama (local LLM, no API key needed) ──────────────────────────
# ollama==0.2.1                # uncomment to use local models via Ollama
